//Script for the tasks to be delivered in week 4

class AccountInfo {
    constructor ( clientNumber, clientName, amount ) {
        clientNumber = this.clientNumber;
        clientName = this.clientName
        amount = this.amount 
    }

    amountNow(amount) {
        amount = this.amount 
        return amount
    }
}

class ChildAccount extends AccountInfo {
    
}